import type { Meta, StoryObj } from '@storybook/react';

import { MarkdownSkeleton } from './MarkdownSkeleton';

const meta = {
  title: 'Markdown/Skeleton ',
  component: MarkdownSkeleton,
  tags: ['autodocs'],
} as Meta;

export default meta;
type Story = StoryObj;

export const Default: Story = {};
